<p id="notice"><%= notice %></p>

<div>
    <%# ログインしているときは以下を表示する %>
    <% if user_signed_in? %>
      <div class="greeting">
        <%= "こんにちは、" %>
        <%= link_to current_user.name + "さん", user_path(current_user.id), class: :greeting__link%>
      </div>
    <% end %>
    <%# // ログインしているときは上記を表示する %>
</div>

<%= month_calendar events: @events do |date, events| %>
  <%= date.day %>

    <% events.each do |event| %>
      <div class = "pain">
        <%= link_to event.pain.name, user_event_path(event.id), class: "btn btn-success" %>
      </div>
    <% end %>
<% end %>
<div class = "new-button">
<%= link_to '経過観察', new_user_event_path %>
</div>